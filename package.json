{
  "name": "cnMarket",
  "version": "1.0.0",
  "description": "NFT minting and marketplace dApp on Celo",
  "scripts": {
    "build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build && rm -rf .next && cp -R frontend/.next .next",
    "vercel-build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build && rm -rf .next && cp -R frontend/.next .next",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:alfajores": "hardhat run scripts/deploy.js --network alfajores",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network celo"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.0",
    "next": "^14.2.35"
  }
}
